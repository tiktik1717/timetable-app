# הוסף קובץ לפרויקט נוכחי

נוסף לתפריט קובץ כפתור:
`הוסף קובץ לפרויקט נוכחי`

## התנהגות
- מקבל קובץ JSON סטנדרטי של הפרויקט.
- בודק שקיימים `schoolData` ו-`schedule`.
- טוען את מצב המערכת מהקובץ והופך אותו מיד למצב הפעיל.
- טוען גם `teacherHighlights` כחלק ממצב התצוגה של הקובץ.
- מתעלם במכוון מכל `checkpoints`, `currentCheckpointId`,
  `activeCheckpointId` ו-`comparisonCheckpointId` שמגיעים מקובץ המקור.
- אינו מוחק ואינו מחליף את `checkpoints` של הפרויקט הנוכחי.
- מאפס את `currentCheckpointId`, משום שהמצב שיובא עדיין אינו נקודת שמירה
  של הפרויקט הנוכחי.
- משאיר את `comparisonCheckpointId` של הפרויקט הנוכחי ללא שינוי.
- מנקה Undo/Redo כדי שלא ניתן יהיה לבצע Undo אל מצב שהיה שייך לקובץ אחר.
- מסמן את פרויקט הענן הנוכחי כבעל שינויים שלא נשמרו.
- לאחר הייבוא ניתן להשתמש במנגנון הקיים "צור נקודת שמירה" כדי לשמור
  את המצב המיובא כנקודת שמירה חדשה בפרויקט הנוכחי.

## הבחנה חשובה
`טען פרויקט מקובץ` ממשיך לפעול כפי שפעל קודם: הוא מחליף גם את נקודות
השמירה בהתאם לקובץ.
`הוסף קובץ לפרויקט נוכחי` מחליף רק את המצב הפעיל ושומר את היסטוריית
נקודות השמירה של הפרויקט שכבר פתוח.
